@model Web.FrontEnd.Models.Account.ForgetPasswordModel
@using Web.FrontEnd.App_Code;

@{
    Layout = "~/Views/Shared/_Inside.cshtml";
}

@section BreadCrumbSection{
    <li class="active">
        <a href="@LinkManager.FORGET_PASSWORD">
            Quên mật khẩu
        </a>
    </li>
}

@section ContentSection{

    <div class="detail_content">

        @if (Model != null)
        {
            using (Html.BeginForm(null, null, FormMethod.Post))
            {
                <table class="tbForm">
                    <tr>
                        <td colspan="2" align="center">
                            <span class="message @(Model.IsValid ? "white" : "red")" style="font-weight:bold">
                                @Html.Raw(Model.Message)
                            </span>
                        </td>
                    </tr>
                    <tr>
                        <td class="left">Tài khoản</td>
                        <td>
                            @Html.TextBoxFor(x => x.Username, new { @class = "form-control m-bot15", required = "" })
                        </td>
                    </tr>
                    <tr>
                        <td class="left">Email</td>
                        <td>
                            @Html.TextBoxFor(x => x.Email, new { @class = "form-control m-bot15", required = "" })
                        </td>
                    </tr>

                    <tr>
                            <td class="left"></td>
                            <td>
                                <img id="img-captcha-register" src='/Captcha/' />
                                <a class="btn-captcha-register" onclick="return ReloadCaptcha('img-captcha-register', '/Captcha/');">
                                    <img src="/Content/images/website/change.png" alt="Thay hình khác" width="20px" />
                                </a>
                            </td>
                        </tr>
                        <tr>
                            <td class="left">Mã xác nhận</td>
                            <td>@Html.TextBoxFor(x => x.Captcha, new { @class = "form-control shot popup_register_captcha", required = "" })</td>
                        </tr>

                    <tr>
                        <td class="left"></td>
                        <td>
                            <input type="submit" value="Xác nhận" class="btn-accept" />
                            <br /><br />
                            <a href="@LinkManager.REGISTER" class="red">Đăng ký</a> |
                            <a href="@LinkManager.LOGIN" class="red">Đăng nhập</a>
                        </td>
                    </tr>
                </table>
            }
        }
    </div>
}

